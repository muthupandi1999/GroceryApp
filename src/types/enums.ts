export enum Status {
  Active = "Active",
  Inactive = "Inactive",
}

export enum Label {
  Home = "Home",
  Work = "Work",
  Other = "Other",
}

export enum Role {
  admin = "admin",
  user = "user",
  subAdmin = "subAdmin",
  deliveryBoy = "deliveryBoy",
}

export enum isFeatured {
  Yes = "Yes",
  No = "No"
}

export enum DiscountType {
  Fixed = "Fixed",
  Percentage = "Percentage",
}

export enum Unit {
  gm = "gm",
  kg = "kg",
  pcs = "pcs",
  ml = "ml",
  ltr = "ltr",
  pack = "pack",
  each = "each",
}